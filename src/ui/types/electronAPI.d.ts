/* declare global {
  interface Window {
    electronAPI: {
      closeWindow: () => void
      minimizeWindow: () => void
      maximizeWindow: () => void
      saveConfig: (config: Record<string>) => Promise<boolean>
      loadConfig: () => Promise<Record<string>>
    }
  }
}
 */
